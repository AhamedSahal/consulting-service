// Legacy entrypoint kept for backwards compatibility.
// New code should start the server via `src/server.js`.
// This file simply delegates to the new server module.

// eslint-disable-next-line global-require
require('./src/server');
